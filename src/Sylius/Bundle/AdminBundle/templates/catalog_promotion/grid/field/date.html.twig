<div class="text-center fs-5">
    {% if data is null %}
        <span class="text-secondary">-</span>
    {% else %}
        <div class="text-nowrap">{{ data|date('d-m-Y') }}</div>
        <div>{{ data|date('H:i') }}</div>
    {% endif %}
</div>
